package com.example.numad21s_firebase_team_puzzlers.services;

import com.example.numad21s_firebase_team_puzzlers.model.User;
import com.google.firebase.database.DatabaseReference;
import com.google.firebase.database.FirebaseDatabase;

public class MessageService {
    public void getAllMessagesBetween(FirebaseDatabase db, User user1, User user2) {
    }

    public void createNewMessage(FirebaseDatabase db, User userFrom, User userTo, int emojiID)
    {
    }
}
